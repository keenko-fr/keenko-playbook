---
__default__: minor
---

Move Convex integration configuration and local development orchestration to the workspace root while preserving `packages/backend/convex` as the sole Convex source location.

Let `bun run dev` establish Convex-managed local deployment state before starting the Nx application processes, and keep generation, validation, and builds valid without `.env.local`.

Add an Nx migration for existing generated repositories whose Keenko-owned lifecycle surfaces still match the prior baseline.
