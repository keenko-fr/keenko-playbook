---
__default__: minor
---

Move Convex integration configuration and local development orchestration to the workspace root while preserving `packages/backend/convex` as the sole Convex source location.

Let `bun run dev` establish Convex-managed local deployment state before starting the Nx application processes, and keep generation, validation, and builds valid without `.env.local`.

Establish Keenko `1.0.0` as the first supported project compatibility baseline. Before `1.0.0`, recreate disposable dogfood projects from the current release candidate when the canonical generated state changes; do not ship migrations for earlier `0.x` project state.

Align the Effect runtime and Node platform prerelease tuple so a fresh consumer resolves one compatible release-candidate generation.

Remove Nx's `.editorconfig` from fresh output, normalize the retained TanStack and React dependencies to Keenko's exact compatibility tuple, and keep the root React Start dependency only as Convex's framework-detection marker.
