---
__default__: minor
---

Add WorkOS AuthKit to every freshly generated Keenko application through the official TanStack Start SDK, Convex-managed development provisioning, and the official Convex WorkOS component.

Keep normal generation and `bun run check` independent of credentials and live WorkOS services, while providing a separate headed email/password authentication smoke flow for provisioned projects.

This changes the pre-1.0 generated baseline. Recreate pre-1.0 consumers and Playground from the release candidate instead of adding migration machinery.
